using System;

namespace Ebrahim.Blog.ViewModels.Type.Identity
{
    public class CustomRoles
    {
        public const string Admin = nameof(Admin);
        public const string User = nameof(User);
        public const string Editor = nameof(Editor);
    }
}
